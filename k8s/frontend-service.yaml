apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: fedex
  labels:
    app: frontend
    app.kubernetes.io/part-of: fedex-notification-service
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
